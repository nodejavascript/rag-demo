node_modules/
dist/
site/app.js
site/consent.js
# 🔴 generated by `tsc -p tsconfig.site.json`, exactly like app.js and consent.js above, and
# therefore not tracked — the image builds it in its own build stage. It was briefly tracked
# (commit 4d28478) because it was added by hand; the repository's rule is that the build
# produces it, and a generated file in git is a file that can be edited where nobody looks.
site/faults.js
data/
secrets/
*.log
.env
.DS_Store
